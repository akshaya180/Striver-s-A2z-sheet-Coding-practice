class Solution {
    public int largestElement(int[] nums) {

        int top=nums[0];

        for(int i=0;i<nums.length;i++)
        {
            if(top < nums[i])
            {
                top = nums[i];
            }

        }
        return top;
    
    }
}
